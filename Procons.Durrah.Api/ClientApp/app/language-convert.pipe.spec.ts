import { LanguageConvertPipe } from './language-convert.pipe';

describe('LanguageConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
